<?xml version="1.0" encoding="utf-8"?>
<manifest package="ru.jvdev.demoapp.client.android"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".DemoApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/title_activity_login"/>
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".activity.user.UserEditActivity"
            android:label="@string/title_new_user"/>
        <activity
            android:name=".activity.user.UserDetailsActivity"
            android:label="@string/title_user_details"/>
        <activity
            android:name=".activity.task.TaskEditActivity"
            android:label="@string/title_new_task"/>
        <activity
            android:name=".activity.task.TaskDetailsActivity"
            android:label="@string/title_task_details"/>
        <activity android:name=".activity.ProfileActivity"
            android:label="@string/title_profile"/>
    </application>

</manifest>