<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:checked="true"
            android:id="@+id/nav_tasks"
            android:icon="@drawable/ic_content_paste"
            android:title="@string/prompt_tasks" />
        <item
            android:id="@+id/nav_users"
            android:icon="@drawable/ic_group"
            android:title="@string/prompt_users" />
    </group>

    <group android:id="@+id/settings">
        <item
            android:id="@+id/nav_logout"
            android:icon="@drawable/ic_exit_to_app"
            android:title="@string/prompt_logout" />
    </group>

</menu>
